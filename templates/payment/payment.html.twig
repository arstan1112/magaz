{% extends 'base.html.twig' %}

{% block title %}Magaz Payment{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('stripe_payment_style') }}
{% endblock %}

{% block body %}
    <br>
    <input id="clientSecret" type="hidden" value="{{ clientSecret }}">
    <div class="container-sm stripe">
{#        <form  action="{{ path('webhook') }}" method="post" id="payment-form">#}
            <div class="form-row">
    {#            <label for="card-element">#}
    {#            </label>#}
                  <label>
                    Credit or debit card
                    <div id="card-element"></div>
                  </label>
                <div id="card-errors" role="alert"></div>
            </div>
            <button id="submit" style="width: auto; background-color: #28a745">Submit Payment</button>
{#        </form>#}

    </div>

{% endblock %}

{% block javascripts %}
{#     <script src="https://js.stripe.com/v3/"></script>#}
     {{ encore_entry_script_tags('stripe_payment') }}
{% endblock %}
